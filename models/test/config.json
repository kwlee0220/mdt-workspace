{
  "id": "test",
  "managerEndpoint": "http://${env:LOCAL_HOST:-localhost}:12985",
  "instanceEndpoint": "https://${env:LOCAL_HOST:-localhost}:19000/api/v3.0",
  "globalConfigFile": "${env:MDT_HOME}/share/mdt_global_config.json",
  "keyStoreFile": "${env:MDT_HOME}/share/mdt_cert.p12",

  "mdtEndpoints": {
    "mqtt":{
      "subscribers": [
        { "topic": "mdt/test/parameters/Data", "element": "param:Data" }
      ]
    }
  },
  "operations": {
    "programOperations" : [
      { "operation": "AddAndSleep:Operation", "descriptorFile": "AddAndSleep/operation.json" }
    ]
  }
}