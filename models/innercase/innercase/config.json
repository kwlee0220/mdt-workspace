{
  "id": "innercase",
  "managerEndpoint": "http://${env:LOCAL_HOST}:12985",
  "instanceEndpoint": "https://${env:LOCAL_HOST}:19006/api/v3.0",
  "globalConfigFile": "${env:MDT_HOME}/share/mdt_global_config.json",
  "keyStoreFile": "${env:MDT_HOME}/share/mdt_cert.p12",

  "operations": {
    "programOperations" : [
      { "operation": "ProcessOptimization:Operation", "descriptorFile": "ProcessOptimization/command.json" }
    ]
  }
}
